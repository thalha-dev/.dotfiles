set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"
# Set 256 colors
set -s default-terminal 'tmux-256color'

set-option -g default-shell "/usr/bin/fish"

# UNICODE SUPPORT
# $ tmux setenv 'LC_ALL' 'en_IN.UTF-8' 
# $ tmux setenv 'LANG' 'en_IN.UTF-8' 
# $ tmux show-environment

#unbind C-b
#set-option -g prefix C-a
#bind-key C-a send-prefix
unbind C-b
set-option -g prefix M-a
bind-key M-a send-prefix
set -g mouse on

set -g status-style 'bg=#000000 fg=#F76E11'

bind r source-file ~/.tmux.conf
set -g base-index 1
set -g pane-base-index 1


# set window split
bind-key v split-window -h
bind-key b split-window

# Set new panes to open in current directory
bind c new-window -c "#{pane_current_path}"
#bind '"' split-window -c "#{pane_current_path}"
bind b split-window -c "#{pane_current_path}"
#bind % split-window -h -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"



# Set vi key bindings mode
set -g mode-keys vi
set -g status-keys vi

# set -g terminal-overrides 'xterm*:smcup@:rmcup@'

#set-window-option -g mode-keys vi
#bind -T copy-mode-vi v send-keys -X begin-selection
#bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# Resizing pane
 bind -r C-k resize-pane -U 8
 bind -r C-j resize-pane -D 2
 bind -r C-h resize-pane -L 2
 bind -r C-l resize-pane -R 8


#mouse mode



# Moving window
bind-key -n C-S-Left swap-window -t -1 \; previous-window
bind-key -n C-S-Right swap-window -t +1 \; next-window


# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'
set -g @plugin 'tmux-plugins/tmux-resurrect'


# -------------------------------------------------------
# NOVA
# -------------------------------------------------------
set -g @plugin 'o0th/tmux-nova'
set -g @plugin 'tmux-plugins/tmux-battery'
# set -g @plugin 'kristijanhusak/tmux-simple-git-status'

### THEME ###
# set -g @nova-nerdfonts true
# set -g @nova-nerdfonts-left       
# set -g @nova-nerdfonts-right  

set -g @nova-pane "#I #W"
# one of: "left", "right", "centre", "absolute-centre"; default: "left"
set -g @nova-pane-justify "left"
# set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"
# set -g @nova-pane "#S:#I:#W" 
set -g @nova-rows 0

### COLORS ###
b_bg="#504945"

seg_a="#a89984 #282828"
seg_b="$b_bg #ddc7a1"

inactive_bg="#32302f"
inactive_fg="#ddc7a1"
active_bg=$b_bg
active_fg="#ddc7a1"

set -gw window-status-current-style bold
# set -g "@nova-status-style-bg" "$inactive_bg"
set -g "@nova-status-style-bg" "#000000"
# set -g "@nova-status-style-fg" "$inactive_fg"
set -g "@nova-status-style-fg" "#EEEDDE"
# set -g "@nova-status-style-active-bg" "$active_bg"
set -g "@nova-status-style-active-bg" "#2C3333"
# set -g "@nova-status-style-active-fg" "$active_fg"
set -g "@nova-status-style-active-fg" "#fafafa"

set -g "@nova-pane-active-border-style" "#44475a"
set -g "@nova-pane-border-style" "#827d51"

### STATUS BAR ###
set -g @nova-segment-prefix "#{?client_prefix,!,o}"
set -g @nova-segment-prefix-colors "#191A19 #ffffff"

set -g @nova-segment-session "#{session_name}"
set -g @nova-segment-session-colors "#191A19 #ffffff"

# set -g @nova-segment-mode "#{?client_prefix,!,.}"
# set -g @nova-segment-mode-colors "#000000 #ffffff"

set -g @batt_icon_status_charging '↑'
set -g @batt_icon_status_discharging '↓'
set -g @nova-segment-battery "#{battery_icon_status} #{battery_percentage}"
set -g @nova-segment-battery-colors "#191A19 #ffffff"

# set -g @nova-segment-whoami "#(whoami)@#h"
# set -g @nova-segment-whoami-colors "$seg_a"

# set -g status-left "#{simple_git_status}"
# set -g @nova-segment-git "#{simple_git_status}"
# set -g @nova-segment-git-colors "#191A19 #ffffff"

set -g @nova-segments-0-left "prefix"
set -g @nova-segments-0-right " session battery"

# -------------------------------------------------------
# GRUVBOX
# -------------------------------------------------------
# set -g @plugin 'egel/tmux-gruvbox'
# set -g @tmux-gruvbox 'dark' # or 'light'
# -------------------------------------------------------
# NORD
# -------------------------------------------------------
# set -g @plugin "arcticicestudio/nord-tmux"
# -------------------------------------------------------
# DRACULA
# -------------------------------------------------------
# set -g @plugin 'dracula/tmux'
#set -g @dracula-show-powerline true
# set -g @dracula-show-fahrenheit false
# set -g @dracula-show-network false
set -g @dracula-show-left-icon session
# set -g @dracula-cpu-usage false
# set -g @dracula-ram-usage false
# set -g @dracula-day-month false
# set -g @dracula-military-time false
# set -g @dracula-show-flags true
# set -g @dracula-show-timezone false
# available plugins: battery, cpu-usage, git, gpu-usage, ram-usage, network, network-bandwidth, network-ping, weather, time
set -g @dracula-plugins "git"
# available colors: white, gray, dark_gray, light_purple, dark_purple, cyan, green, orange, red, pink, yellow
# set -g @dracula-[plugin-name]-colors "[background] [foreground]"
#set -g @dracula-cpu-usage-colors "pink dark_gray"
set -g @dracula-git-colors "dark_gray white"
#set -g @dracula-git-disable-status true
# default is ✓. Avoid using non unicode characters that bash uses like $, * and ! 
#set -g @dracula-git-show-current-symbol ✓
# default is unicode !. Avoid bash special characters 
#set -g @dracula-git-show-diff-symbol !
# default is unicode no message
set -g @dracula-git-no-repo-message "Directory"
# -------------------------------------------------------







# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
